---
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: lubelogger
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod-http01-eg
    cert-manager.io/private-key-algorithm: ECDSA
    acme.cert-manager.io/http01-parentreffallback: "true"
spec:
  parentRef:
    namespace: core-eg
    name: eg-http-public
  listeners:
    - name: https
      hostname: lube.risson.space
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          - name: lubelogger-https
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: lubelogger
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: ListenerSet
      name: lubelogger
  hostnames:
    - lube.risson.space
  rules:
    - matches:
        - path:
            value: "/"
      backendRefs:
        - name: lubelogger
          port: 80
