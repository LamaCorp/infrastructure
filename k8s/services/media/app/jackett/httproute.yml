---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jackett
spec:
  parentRefs:
    - name: eg-http-media
      namespace: core-eg
      sectionName: https
  hostnames:
    - jackett.lama-corp.space
  rules:
    - matches:
        - path:
            value: "/"
      backendRefs:
        - name: jackett
          port: 80
      timeouts:
        request: 60s
        backendRequest: 60s
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: jackett
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: jackett
  oidc:
    provider:
      issuer: https://auth.lama-corp.space/application/o/jackett/
    clientID: jackett
    clientSecret:
      name: jackett-oidc-credentials
    scopes:
      - offline_access
    refreshToken: true
